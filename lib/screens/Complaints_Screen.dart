import 'package:flutter/material.dart';

class Complaints_Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('انتقادات'),
      ),
      body: Center(
        child: Text(' صفحه انتقادات  '),
      ),
    );
  }
}
